<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.calma">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DependantRedLog"></activity>
        <activity android:name=".DependantAmberLog" />
        <activity android:name=".DependantGreenLog" />
        <activity android:name=".RedSettings" />
        <activity android:name=".AmberSettings" />
        <activity android:name=".GreenSettings" />
        <activity android:name=".DependantAlertTracker" />
        <activity android:name=".TrusteeDashHints" />
        <activity android:name=".DependantDashHints" />
        <activity android:name=".TrusteeEditProfile" />
        <activity android:name=".DependantEditProfile" />
        <activity android:name=".Logout" />
        <activity android:name=".TrusteeContacts" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".AmberMaps"
            android:label="@string/title_activity_amber_maps" />
        <activity
            android:name=".GreenMaps"
            android:label="@string/title_activity_green_maps" />
        <activity android:name=".DependantContacts" />
        <activity android:name=".TrafficLights" />
        <activity android:name=".TrusteeDash" />
        <activity android:name=".DependantDash" />
        <activity android:name=".TipsAndHintsActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>